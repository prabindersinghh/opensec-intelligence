# Launch Copy — OpenSec Intelligence

## Pinned Tweet / LinkedIn Hook

I built the security layer that Claude Code doesn't have.
4 AI agents. Runs free on your laptop. Finds and fixes
vulnerabilities across your entire codebase.

No API keys. No cloud. No cost.

npm install -g opensec-intelligence

github.com/prabindersinghh/opensec-intelligence

---

## Hacker News (Show HN)

**Show HN: OpenSec Intelligence – local-first 4-agent security engine (free, Ollama)**

Claude Code writes your code. I built what makes sure it doesn't get hacked.

4 AI agents running fully on your machine:
- Scanner maps your attack surface (code, Docker, k8s, Terraform, secrets)
- Analyst finds real vulnerabilities (OWASP Top 10, cross-modal correlation)
- Consensus re-examines every HIGH/CRITICAL independently — filters below 0.7 confidence
- Fixer writes the exact patch, shows before/after diff, asks approval, commits

Zero data leaves your machine. Free forever with Ollama.

https://github.com/prabindersinghh/opensec-intelligence

---

## Reddit (r/netsec / r/programming)

**I built the security scanner I always wished Claude Code had**

Tired of AI coding tools that write vulnerable code and call it done. So I built
OpenSec Intelligence — a 4-agent pipeline that scans, validates with consensus,
and writes the exact fixes. Runs locally with Ollama, completely free.

```
npm install -g opensec-intelligence
opensec scan ./
```

Finds: SQLi, command injection, SSRF, secret leakage, weak auth, infra misconfigs.
Validates: 3+ models independently agree before flagging anything.
Fixes: exact diffs, approval-gated, git-committed.

Repo: https://github.com/prabindersinghh/opensec-intelligence

---

*By Prabinder Singh — B.Tech CS, Thapar Institute · Founder, Leorit.ai*
